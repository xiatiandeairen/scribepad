# 示例:auth 重构计划

当前业务背景是：现有的 session token [改写] [改写] 直接存放在浏览器 cookie 中，而合规团队在 2026 年第二季度提出，需要将其重构为符合 SOC2 要求的会话管理方案。核心诉求是会话能够即时撤销，且敏感载荷不暴露在客户端。后续章节将围绕几种候选方案（服务端 Session、OAuth2 + OIDC、不透明 token）展开对比，并最终在「待决策」处收敛选型。

涉及范围:web 端登录、API 网关鉴权、第三方 OAuth 回调。

## 方案 B:服务端 Session(Redis)

核心思路:登录成功后,服务端在 Redis 中创建一条 session 记录(包含用户身份、过期时间、设备信息等),再将对应的 session ID 下发给客户端(通常放在 cookie 中)。后续每次请求,网关用 session ID 去 Redis 查实际状态,而不是直接信任客户端携带的 token 载荷。

优点:
- 可即时撤销:登出、风控、密码修改时,直接删 Redis 记录,所有端立刻失效,无需等 token 过期
- 客户端只持有一个不透明 ID，不再携带用户信息载荷，因而请求体积更小，也能降低敏感信息泄露风险
- 易于审计:所有会话集中在 Redis,可统一查询活跃会话、强制下线、追踪异常登录

缺点:
- 需维护会话存储:Redis 的高可用、持久化、容量规划都要额外投入,挂了等于全站登录态丢失
- 跨域/多端同步成本高:Web、移动端、第三方 OAuth 回调要共用同一套 session 协议,跨域 cookie、SameSite、CSRF 防护都要重新设计

适用场景:对撤销实时性、审计合规要求高,且团队有运维 Redis 集群能力的场景——和当前 SOC2 合规诉求比较契合。

## 方案 C:OAuth2 + OIDC(委托给 IdP)

- 优点:复用成熟身份提供商 [改写] [改写] [改写] [改写]、支持 SSO 与多因素
- 缺点:接入复杂、依赖外部可用性、调试链路长

## 方案 D:不透明 token + 引用查表

- 优点:撤销简单、不暴露载荷、易于轮换
- 缺点:每次校验需查存储、对中心化服务有依赖
## 方案 B:Redis-backed session

- 优点:撤销简单、可控
- 缺点:多 region 一致性问题

## 待决策

挑一个,下周三前给出。
