弱结构 review 文档：没有 H1，靠 review 专属节名（裁决 + 遗留 ≥2 个）投票命中 docKind。
各节形状均有残缺，用于验证抽取降级不 throw。

## 需要你裁决

引导段：本节没有任何规范 H3 单元，下面这个 H3 缺 tag 和字段。

### D1. 只有一句决策，没有任何 bullet 字段

### 没有标签的 H3 单元应被跳过或降级

- **背景**：这条挂在无标签 H3 下

## 3. 声明与证据

| # | 声明 |
| --- | --- |
| C1 | 表格缺证据列和核验列 |
| 没有标签的行 | 应降级处理不 throw |

## 4. 遗留与假设

- **L1 [自造类别]** 未知 kind 应归 unknown
- 没有粗体标签的裸列表项
- **L2** 缺 kind 括号，只有标签 —— 触发条件：仍应解析出 condition
